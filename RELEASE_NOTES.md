## 🚀 rapoo-tray v1.2.0 发行说明 (Release Notes)

rapoo-tray v1.2.0 针对 v1.1.0 进行了全面的架构加固、交互升级与硬件兼容策略重构。本版本核心更新内容如下：

### 一、硬件兼容与设备识别策略重构
1. **确立实机验证准则**：彻底移除 1.1.0 中基于推测的 VT9 PID 数据，坚持仅收录具备真实硬件测试数据的设备。
2. **新增设备实机支持**：合并社区 PR #3，正式收录雷柏 VT3 MAX（无线接收器 PID `0x1417`）。
3. **二代 Nordic 统一架构即插即用**：针对采用 Nordic 方案（54L15 / 3950）的雷柏二代游戏鼠标，未单独打标机型自动归入“雷柏游戏鼠标 (通用)”，电量监测、DPI 切换、回报率调节、休眠配置等全功能 100% 完整支持。
4. **模块化扩展表**：重构设备静态映射表（`VERIFIED_MODELS`），新增设备仅需提供 PID 即可快速收录。

### 二、系统托盘电池图标个性化体系
1. **风格循环切换**：支持左键双击任务栏托盘图标无缝循环切换 3 种电池展示风格，偏好自动持久化保存至注册表：
   - **配置一（经典长条电池）**：外框与数字全面放大两号（高度提升至 14px，内嵌数字升级为 5x9 点阵大号字体），显著提升在 Windows 任务栏下的可视度。
   - **配置二（状态大圆点）**：纯净无干扰呼吸状态圆点（不含数字），极简视觉呈现。
   - **配置三（大号矢量数字）**：采用 4 倍超采样（SSAA）抗锯齿算法，彻底消除文字毛边与锯齿。
2. **状态变色机制**：三档变色指示（$\le 30\%$ 警示红、$> 30\%$ 科技蓝、充电中翡翠绿、离线/休眠虚线或横杠）。

### 三、DPI 动态自适应屏幕悬浮窗 (OSD) 重构
1. **动态自适应卡片**：废弃固定宽高等比缩放，重构为基于 `DT_CALCRECT` 文本长度与系统 DPI 的毫秒级动态自适应卡片。
2. **全新三行信息架构**：
   - **第一行（型号与主数值）**：设备全称与当前 DPI（例如：`雷柏 VT7  DPI 800`）。
   - **第二行（双轴独立数值）**：X 轴与 Y 轴独立分辨率（例如：`X 轴: 800    Y 轴: 800`）。
   - **第三行（状态汇总行）**：连接模式、当前档位、电量与回报率（例如：`2.4G  |  第 1 档  |  🔋 37%  |  1000 Hz`）。
3. **符号化电量状态**：未充电时以 `🔋` 符号紧凑展示，充电状态自动切换为 `⚡` 符号（例如：`⚡ 85%`）。
4. **交互全覆盖**：切换 DPI、调节滑块、切换回报率、左键单击托盘图标均实时触发对应 OSD 反馈。

### 四、Fluent 亚克力右键控制面板与交互体验
1. **视觉纯净度提升**：结合 DWM 物理圆角与亚克力（Acrylic）模糊背景，消除边缘白色杂质与直角毛刺，适配 Windows 10/11 深浅主题。
2. **菜单电量显示规范化**：顶栏电量项规范显示真实电量与状态（例如：`85% 充电中`），去除多余图标。
3. **硬件状态实时同步**：打开右键菜单时，自动通过底层硬件寄存器读取当前真实回报率并高亮匹配。
4. **2~120 分钟平滑休眠调节**：集成连续平滑拖动条，支持滚轮步进与 5m/10m/15m/30m/60m 快捷预设芯片按钮，设置即时下发硬件固件。

### 五、底层双模热插拔通信与系统稳定性加固
1. **HID 通信端点仲裁重构**：重写端点枚举逻辑，严格区分 Usage `0x000E`（控制通道）、Usage `0x000F`（特性通道）与 Usage `0x0002`（状态广播通道），彻底解决 1.1.0 在 2.4G 接收器与有线同时接入时的端点吞没与无法通信缺陷。
2. **双模智能优先级**：接入有线瞬间自动优先绑定有线通信端点，拔出即时无缝回退至 2.4G 无线。
3. **线程安全互斥锁**：在 `Device::State` 状态机中引入 `CRITICAL_SECTION` 互斥锁，消除 HID 监听后台线程与 UI 主线程之间的竞态风险。
4. **模态消息防吞修复**：修复右键菜单模态循环中 `WM_QUIT` 消息处理逻辑，确保点击“退出程序”时主线程干净退出。
5. **硬件端点容错回退**：针对控制端点添加只写/只读多级共享容错，避免与第三方软件产生排他性句柄冲突。
