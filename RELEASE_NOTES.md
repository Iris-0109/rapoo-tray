## 🚀 rapoo-tray v1.3.0 发行说明 (Release Notes)

rapoo-tray v1.3.0 带来了全新的**原生 Win11 低电量预警浮窗系统**、全面重构复刻的**现代托盘电池图标体系**，以及任务栏自适应吸附与架构稳定性强化。

---

### 一、全新低电量预警浮窗系统 (Low-Battery Alert Window)
1. **智能分级预警机制**：
   - 剩余电量 $\le 20\%$ 时，弹出醒目的黄色电量不足提醒卡片；
   - 剩余电量 $\le 10\%$ 时，升级为红色严重低电量警告卡片，提醒立即充电。
   - 充电状态识别：接入 USB 线缆或底座充电时，告警卡片自动静默关闭；充回 $25\%$ 以上自动重置触发标志。
2. **任务栏动态贴合 (Taskbar Dynamic Docking)**：
   - 通过 Win32 Shell API `SHAppBarMessage(ABM_GETTASKBARPOS)` 动态获取系统任务栏的真实屏幕坐标与停靠边缘。
   - 自适应用户任意任务栏高度（40px/48px/60px/72px 等）及位置（底部/顶部/左侧/右侧），确保卡片紧密平贴任务栏边缘（8px 精致间距），避免传统绝对坐标悬空或被遮挡。
   - 多显示器感知：自动锁定任务栏所在的显示器，支持多屏工作环境。
3. **DWM 硬件级亚像素平滑圆角与原生着色**：
   - 采用 Windows 11 `DWMWA_WINDOW_CORNER_PREFERENCE = DWMWCP_ROUND` GPU 硬件抗锯齿圆角及原生阴影。
   - 通过 `DWMWA_BORDER_COLOR` 原生绘制平滑警告色边框，彻底根除 GDI `SetWindowRgn` 带来的锯齿、杂色外溢与毛边。
4. **游戏/全屏免打扰设计**：
   - 窗口采用 `WS_EX_NOACTIVATE` 属性，弹窗时不抢占用户正在使用的应用/游戏键盘鼠标焦点。
   - 支持快捷关闭按钮与悬浮高亮 `[我知道了]` 交互。

---

### 二、三大现代托盘电池样式重构 (Tray Battery Styles Overhaul)
1. **样式一：经典纯净电池胶囊 (Battery)**
   - 饱满外框与呼吸内边距设计，电量填充带圆角过渡。
   - 充电状态下电池内部居中叠加纯白微型闪电符号。
2. **样式二：360° 仪表盘环形电量进度 (Ring)**
   - 升级为 3.0px 加粗弧线，顺时针展开，圆弧两头采用半圆形圆角收口胶囊端。
   - 充电时居中呈现纯白闪电符号，未连接时呈现优雅微暗虚线底轨。
3. **样式三：大号数字 + 底部比例微横轨 (Number & Mini Bar)**
   - 采用大号 Segoe UI 粗体数字，光学基线居中对齐。
   - 底部新增 2px 细微电量比例横轨，即使在微型图标下也能一眼识别准确电量。

---

### 三、底层架构与稳定性优化 (Architecture & Stability)
1. **UI 线程与 HID 协议线程解耦**：
   - 弹窗窗口句柄由主 GUI 线程统一初始化与托管消息循环，与 HID 阻塞式轮询工作线程解耦，彻底根除任何黑屏、白屏或无响应假死隐患。
2. **多进程独立防护**：
   - 测试版与主程序互斥体隔离，避免多环境启动冲突。

---

### 四、组件版本同步
- 主程序 `rapoo-tray.exe`、安装程序 `rapoo-tray-setup.exe` 及 TrafficMonitor 扩展插件 `rapoo-plugin.dll` 同步升级至 **v1.3.0**。
